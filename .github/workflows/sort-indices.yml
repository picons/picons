name: Sort srp.index and utf8snp.index

on:
  workflow_dispatch:

jobs:
  sort-index:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Run SRP sorting script
        run: python resources/tools/sort_srp_index.py

      - name: Run UTF8 SNP sorting script
        run: python resources/tools/sort_utf8snp_index.py

      - name: Commit changes (if any)
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          if git diff --quiet; then
            echo "No changes to commit."
          else
            git add build-source/srp.index build-source/utf8snp.index
            git commit -m "Auto: sort srp.index and utf8snp.index via GitHub Action"
            git push --force
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
